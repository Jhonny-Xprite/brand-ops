# Auto-generated by Apex — do NOT edit manually
# Re-generated on: 2026-04-04T12:00:00Z
# Trigger: first-activation

project_dna:
  extracted_at: "2026-04-04T12:00:00Z"
  project_name: "brand-ops"

  stack:
    framework: "next.js"
    ui: "react"
    styling: "tailwindcss"
    animation: "css-only"
    state: "redux-toolkit"
    testing: "jest"

  design_language:
    style: "flat-premium"
    colors:
      primary: "#2563eb" # var(--action-primary)
      secondary: "#f8fafc" # var(--action-secondary)
      surface: "#ffffff" # var(--surface-default)
      text: "#0f172a" # var(--text-default)
    spacing_unit: "4px"
    radius_default: "8px" # var(--radius-md)
    shadow_style: "subtle"
    dark_mode: "supported" # detected some vars, but primary focus is light in current css
    theme_mechanism: "css-vars"

  conventions:
    file_org: "type-based"
    component_naming: "PascalCase"
    folder_naming: "kebab-case"
    export_style: "default" # detected in tailwind config export default
    props_style: "inline/interface"
    import_style: "relative"
    import_alias: "@/"

  routing:
    router: "next/pages"
    structure: "file-based"
    api_pattern: "route-handlers" # inferred

  quality:
    eslint_config: "custom"
    ts_strict: "true"
    test_framework: "jest"
    test_style: "testing-library"
    ci: "github-actions"

  # Profile selection
  apex_profile: "web-next"

  # Agent-specific context
  agent_context:
    css-eng:
      styling_lib: "tailwindcss"
      design_tokens: "css-vars"
      dark_mode: "media-query"
    react-eng:
      react_version: "18"
      rsc_enabled: "false" # Pages router
      state_management: "redux-toolkit"
    motion-eng:
      animation_lib: "css-only"
      reduced_motion: "implemented"
    a11y-eng:
      aria_usage: "basic"
      focus_management: "implemented"
    perf-eng:
      bundler: "webpack" # next/pages
      lazy_loading: "partial"
      image_optimization: "next-image"
