/**
 * Redux Slices
 * Slices will be created here as features are implemented
 *
 * Example slice structure:
 * - creativesSlice.ts (for Creative asset management)
 * - metadataSlice.ts (for Metadata management)
 * - uiSlice.ts (for UI state - modals, sidebars, etc)
 */

// Export slices here as they are created
// export { default as creativesSlice } from './creativesSlice'
// export { default as metadataSlice } from './metadataSlice'
