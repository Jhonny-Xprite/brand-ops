---
template:
  id: story-0.2
  version: 1.0
  
metadata:
  epic: 1
  story_num: 2
  title: "Project Structure & TypeScript Types"
  priority: P0
  effort_minutes: 30
  
---

# Story 0.2: Project Structure & TypeScript Types

## Status
Draft

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - npm run typecheck
  - npm run build
  - coderabbit --prompt-only -t uncommitted
```

## Story

**As a** developer,  
**I want** a clean Next.js project structure with TypeScript types for data models,  
**so that** I can start building features with proper type safety and atomic design organization

---

## Acceptance Criteria

- [ ] Folder structure matches `docs/architecture/source-tree.md` (atomic design: atoms, molecules, organisms)
- [ ] `src/lib/types/` created with TypeScript interfaces for Creative, Metadata, Version, SyncMetadata
- [ ] Redux store scaffold in `src/store/` with slices folder ready
- [ ] RTK Query api skeleton in `src/store/api/` (placeholder endpoints)
- [ ] Components folder structure: `atoms/`, `molecules/`, `organisms/` (empty, ready for components)
- [ ] Pages folder structure: `pages/` or `app/` (Next.js router ready)
- [ ] `.env.local` updated with all required keys (DATABASE_URL, API_URL, LOG_LEVEL, etc.)
- [ ] `tsconfig.json` with strict mode enabled (`strict: true`)
- [ ] `npm run typecheck` passes with no errors
- [ ] `npm run build` succeeds (empty app compiles)

---

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** Frontend (Structure & Types)  
**Secondary Type(s):** Infrastructure/Setup  
**Complexity:** Low (scaffolding only)

### Specialized Agent Assignment

**Primary Agents:**
- @dev (implementation)
- @architect (structure validation)

**Supporting Agents:**
- @ux-design-expert (atomic design pattern review)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run `npm run typecheck` before marking story complete
- [ ] Pre-PR (@architect): CodeRabbit on TypeScript types and folder structure

### Self-Healing Configuration

**Expected Self-Healing:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior:**
- CRITICAL issues: auto_fix (up to 2 iterations)
- HIGH issues: document_only if auto_fix fails

### CodeRabbit Focus Areas

**Primary Focus:**
- TypeScript type correctness and completeness
- Folder structure consistency with atomic design
- Redux store configuration
- No unused imports

**Secondary Focus:**
- Export statement correctness
- Index file organization
- Naming conventions (PascalCase components, camelCase utilities)

---

## Tasks / Subtasks

- [ ] Create Base Folder Structure (AC: 1)
  - [ ] Create `src/components/{atoms,molecules,organisms}/` directories
  - [ ] Create `src/pages/` or `src/app/` directory
  - [ ] Create `src/lib/{types,utils}/` directories
  - [ ] Create `src/store/{api,slices}/` directories
  - [ ] Create `src/hooks/` directory
  - [ ] Create `src/styles/` directory

- [ ] TypeScript Models / Interfaces (AC: 2)
  - [ ] Create `src/lib/types/models.ts` with 4 interfaces:
    - Creative (id, name, type, filePath, fileSize, mimeType, status, tags, notes, createdAt, updatedAt)
    - Metadata (id, creativeId, designer, campaign, targetAudience, format, customFields)
    - Version (id, creativeId, versionNumber, commitHash, message, createdAt)
    - SyncMetadata (id, entityId, lastSyncAt, syncHash, isSynced)
  - [ ] Create `src/lib/types/index.ts` - export all interfaces
  - [ ] Verify interface fields match Prisma schema from Story 0.1

- [ ] Redux Store Setup (AC: 3)
  - [ ] Create `src/store/index.ts` with `configureStore()`
  - [ ] Configure `combineReducers()` (empty for now, ready for slices)
  - [ ] Export `RootState` type
  - [ ] Export `AppDispatch` type
  - [ ] Create `src/store/slices/` directory (placeholder for future slices)

- [ ] RTK Query API Skeleton (AC: 3)
  - [ ] Create `src/store/api/index.ts` with `createApi`
  - [ ] Configure base query (pointing to local API)
  - [ ] Add placeholder endpoints definition (no implementation yet)
  - [ ] Export API instance

- [ ] Environment Variables (AC: 7)
  - [ ] Update `.env.local` with all required keys:
    - DATABASE_URL (from Story 0.1)
    - NEXT_PUBLIC_API_URL
    - LOG_LEVEL
  - [ ] Add `.env.local` to `.gitignore` (if not already)
  - [ ] Document in `.env.example` template (without values)

- [ ] TypeScript Configuration (AC: 8, 9)
  - [ ] Verify `tsconfig.json` exists
  - [ ] Enable `strict: true`
  - [ ] Enable `noImplicitAny: true`
  - [ ] Configure path aliases: `@/*` → `src/*`
  - [ ] Run `npm run typecheck` - verify no errors

- [ ] Build Verification (AC: 10)
  - [ ] Run `npm run build`
  - [ ] Verify `.next/` folder created
  - [ ] Verify no TypeScript errors
  - [ ] Verify no build warnings

---

## Dev Notes

### Relevant Architecture Context

[Source: architecture/source-tree.md]
- Folder structure: `src/components/{atoms,molecules,organisms}` (atomic design)
- Pages: `src/pages/` (pages router) or `src/app/` (app router)
- Store: `src/store/{api,slices}`
- Types: `src/lib/types/`

[Source: architecture/coding-standards.md§TypeScript Standards]
- No `any` types
- Strict mode required
- All data models have TypeScript interfaces
- Type all function parameters and return values

[Source: architecture/tech-stack.md§Frontend Stack]
- Next.js 14+
- React 18+
- TypeScript (strict mode)
- Redux Toolkit + RTK Query
- Tailwind CSS v3
- shadcn/ui

### Project Structure

```
src/
├── components/
│   ├── atoms/          # Base components (Button, Input, Label, Badge, etc)
│   ├── molecules/      # Simple combinations (FormField, Card, Header, Footer)
│   └── organisms/      # Complex UI sections (Sidebar, MainLayout, FileExplorer)
├── pages/ or app/      # Page routes (empty structure, ready for pages)
├── lib/
│   ├── types/
│   │   ├── models.ts   # Data model interfaces (Creative, Metadata, Version, SyncMetadata)
│   │   └── index.ts    # Export all types
│   ├── utils/          # Helper functions
│   └── prisma.ts       # Prisma Client singleton (created in Story 0.1)
├── store/
│   ├── index.ts        # Redux store configuration
│   ├── api/            # RTK Query endpoints
│   └── slices/         # Redux slices (empty, ready for implementation)
├── hooks/              # Custom React hooks
└── styles/             # Global styles + design tokens
```

### Atomic Design Pattern

[Source: architecture/front-end-spec.md§Atomic Design]
- **Atoms:** Smallest components (Button, Input, Label, Badge, Icon)
- **Molecules:** Simple combinations (Form fields, Cards, Headers)
- **Organisms:** Complex sections (Sidebars, Layouts, Dialogs)
- **Templates:** Page layouts (ready in Story 0.2 structure)
- **Pages:** Specific instances (implemented in later stories)

### Previous Story Context
Story 0.1 (Prisma Setup) provides database schema. Story 0.2 creates TypeScript interfaces that mirror that schema.

### Testing Standards

[Source: architecture/coding-standards.md§Testing Standards]
- Test framework: Jest
- Test location: `src/**/__tests__/*.test.ts`
- For this story: Type definition tests (verify interfaces are correct)
- Coverage target: >70%

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-03 | 1.0 | Story created | @sm (River) |

---

## Dev Agent Record

### Agent Model Used
_To be populated during implementation_

### Debug Log References
_To be populated during implementation_

### Completion Notes List
_To be populated during implementation_

### File List
_To be populated during implementation_

---

## QA Results

_To be populated during QA review_

---

**Story Status:** Draft  
**Created:** 2026-04-03  
**Depends On:** Story 0.1 (Prisma schema from Story 0.1 informs interfaces)  
**Blocks:** Story 1.1 (Epic 1 - components depend on folder structure)  
**Template:** AIOX Story Template v2.0
