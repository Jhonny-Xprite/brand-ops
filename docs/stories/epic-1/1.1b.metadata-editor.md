---
template:
  id: story-template-v2
  version: 2.0

metadata:
  epic: 1
  story_num: "1.1b"
  title: "Metadata Editor"
  priority: P0
  effort_minutes: 1200
  blocker: false
  depends_on: ["1.1a", "PRE-1.1", "PRE-1.2", "PRE-1.3"]
---

# Story 1.1b: Metadata Editor

## Status
Ready

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools:
  - npm run lint
  - npm run typecheck
  - npm test -- --runInBand
  - npm run build
```

## Story

**As a** creative operator,  
**I want** to edit file metadata inline from the creative library,  
**so that** type, status, tags, and notes can be maintained without leaving the file workflow

---

## Acceptance Criteria

1. The metadata editor uses the final `CreativeFile` / `FileMetadata` contract established by `PRE-1.1`.
2. Users can edit type, status, tags, and notes from the creative library without leaving the browsing context.
3. Validation behavior follows the messages and UI patterns from `docs/error-handling-spec.md`.
4. Type and status are required, tags honor the agreed limit, and notes honor the 500-character limit.
5. Tag editing preserves the app contract `tags: string[]`, not the raw persisted storage shape.
6. Save behavior updates the visible file state and persists the metadata in SQLite.
7. Keyboard support covers tab, shift-tab, enter/save, and escape/cancel for the editor flow.
8. The implementation is ready for later auto-versioning rather than inventing its own persistence side path.
9. Lint, typecheck, tests, and build pass after the implementation.

---

## CodeRabbit Integration

### Story Type Analysis

**Primary Type:** Frontend  
**Secondary Type(s):** Form validation, persistence  
**Complexity:** High

### Specialized Agent Assignment

**Primary Agents:**
- @dev
- @architect

**Supporting Agents:**
- @qa
- @po

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Verify metadata persistence, keyboard behavior, and validation UX.
- [ ] Pre-PR (@github-devops): Review changed form logic, tests, and build output.
- [ ] Pre-Deployment (@github-devops): Not applicable for the local-first development story.

### CodeRabbit Focus Areas

**Primary Focus:**
- Form validation and persistence
- Contract correctness for `tags`

**Secondary Focus:**
- Keyboard flow and cancel/save behavior
- Future compatibility with auto-versioning

---

## Tasks / Subtasks

- [ ] Build the metadata editing surface (AC: 1, 2, 7)
  - [ ] Render type, status, tags, and notes editing controls in the library flow.
  - [ ] Implement keyboard navigation and cancel/save behavior.

- [ ] Implement validation and persistence (AC: 3, 4, 5, 6)
  - [ ] Apply the approved field requirements and limits.
  - [ ] Persist metadata to SQLite through the approved app contract.
  - [ ] Keep `tags` array semantics intact at the app boundary.

- [ ] Preserve later versioning compatibility (AC: 8, 9)
  - [ ] Keep save flow compatible with the future versioning queue.
  - [ ] Run lint, typecheck, tests, and build.

---

## Dev Notes

### Relevant Context

[Source: `docs/prd/epic-1-creative-production.md`]
- Feature 1.2 defines the metadata fields, validation expectations, and keyboard behavior.

[Source: `docs/database-schema.md`]
- `FileMetadata` persists tags through the SQLite-compatible representation agreed in `PRE-1.1`.

[Source: `docs/error-handling-spec.md`]
- Validation and save-failure semantics are already standardized and should not be reworded ad hoc.

[Source: `docs/git-integration-flow.md`]
- This story should persist metadata in a way that later versioning can observe, rather than creating an incompatible save path.

### Sequencing Notes

- Execute this after `1.1a`.
- `1.1c` should integrate and harden the file-browser + metadata workflows together before git-backed versioning starts.

### Testing

- Verify validation errors and recovery behavior.
- Verify metadata persistence across reload/app restart.
- Verify keyboard interactions for edit, save, and cancel.
- Run lint, typecheck, tests, and build.

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-03 | 1.1 | Batch validation completed and story promoted to Ready with no blocking PO concerns | @po (Pax) |
| 2026-04-03 | 1.0 | Story created from Epic 1 after PRE blockers completed | @sm (River) |

---

## Dev Agent Record

### Agent Model Used

To be completed by implementation agent.

### Debug Log References

To be completed by implementation agent.

### Completion Notes List

To be completed by implementation agent.

### File List

To be completed by implementation agent.

---

## QA Results

To be completed by QA agent.
