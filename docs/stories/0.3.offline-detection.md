---
template:
  id: story-0.3
  version: 1.0
  
metadata:
  epic: 7
  story_num: 3
  title: "Service Worker & Offline Detection"
  priority: P0
  effort_minutes: 30
  
---

# Story 0.3: Service Worker & Offline Detection

## Status
Draft

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - npm run typecheck
  - npm run build
  - coderabbit --prompt-only -t uncommitted
```

## Story

**As a** developer,  
**I want** a Service Worker registered with basic offline detection,  
**so that** the app can detect offline/online state and handle it gracefully

---

## Acceptance Criteria

- [ ] Service Worker file created at `public/sw.js`
- [ ] Service Worker registered in root layout component
- [ ] `navigator.onLine` check implemented and working
- [ ] Offline status badge component created and visible in header
- [ ] Online/offline event listeners properly attached to window
- [ ] Service Worker logs registration success/failure to console
- [ ] `npm run build` succeeds without errors
- [ ] Manual test: disable network in DevTools, see "🔴 OFFLINE" badge
- [ ] Manual test: re-enable network in DevTools, see "🟢 ONLINE" badge
- [ ] Service Worker persists across page reloads

---

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** Frontend (Infrastructure)  
**Secondary Type(s):** Offline-First Architecture  
**Complexity:** Low (infrastructure setup)

### Specialized Agent Assignment

**Primary Agents:**
- @dev (implementation)
- @architect (service worker patterns)

**Supporting Agents:**
- @ux-design-expert (status badge UI)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run `npm run typecheck && npm run build` before marking story complete
- [ ] Pre-PR (@architect): CodeRabbit on Service Worker code and event listener cleanup

### Self-Healing Configuration

**Expected Self-Healing:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior:**
- CRITICAL issues: auto_fix (up to 2 iterations)
- HIGH issues: document_only if auto_fix fails

### CodeRabbit Focus Areas

**Primary Focus:**
- Service Worker registration and lifecycle
- Event listener cleanup (no memory leaks)
- Error handling for SW registration failures
- SSR-safe hook implementation

**Secondary Focus:**
- Browser API compatibility checks
- Console logging for debugging
- Component re-render optimization

---

## Tasks / Subtasks

- [ ] Create Service Worker File (AC: 1)
  - [ ] Create `public/sw.js`
  - [ ] Implement install handler with `self.skipWaiting()`
  - [ ] Implement activate handler with `clients.claim()`
  - [ ] Implement fetch handler (passthrough, no caching yet)
  - [ ] Add console logging for debugging

- [ ] Register Service Worker (AC: 2, 7)
  - [ ] Add useEffect in root layout component (RootLayout or _app.tsx)
  - [ ] Call `navigator.serviceWorker.register('/sw.js')`
  - [ ] Log success to console with registration details
  - [ ] Log errors to console with error details
  - [ ] Handle when `navigator.serviceWorker` not available (older browsers)

- [ ] Create Offline Detection Hook (AC: 3, 5)
  - [ ] Create `src/hooks/useOnlineStatus.ts`
  - [ ] Initialize with `navigator.onLine` value
  - [ ] Listen to `online` event - set `isOnline = true`
  - [ ] Listen to `offline` event - set `isOnline = false`
  - [ ] Cleanup event listeners on unmount
  - [ ] Handle SSR (check `typeof navigator` before accessing)

- [ ] Create Status Badge Component (AC: 4)
  - [ ] Create `src/components/atoms/OfflineIndicator.tsx`
  - [ ] Use `useOnlineStatus` hook
  - [ ] Display "🟢 ONLINE" when online (green color)
  - [ ] Display "🔴 OFFLINE" when offline (red color)
  - [ ] Add to layout header (visible on all pages)
  - [ ] Style with Tailwind CSS

- [ ] Manual Testing (AC: 8, 9, 10)
  - [ ] Run `npm run dev`
  - [ ] Open DevTools → Network tab
  - [ ] Check "Offline" checkbox
  - [ ] Verify badge displays "🔴 OFFLINE"
  - [ ] Uncheck "Offline" checkbox
  - [ ] Verify badge displays "🟢 ONLINE"
  - [ ] Refresh page while offline - verify SW still registered
  - [ ] Check DevTools → Application → Service Workers - verify registration

- [ ] Build Verification (AC: 6, 7)
  - [ ] Run `npm run typecheck` - verify no errors
  - [ ] Run `npm run build`
  - [ ] Verify `public/sw.js` is included in build
  - [ ] Verify `.next/` created without errors

---

## Dev Notes

### Relevant Architecture Context

[Source: architecture/fullstack-architecture.md§3. Offline Layer (Service Worker + IndexedDB)]
- Service Worker: Cache-first strategy (will be implemented in future epic)
- IndexedDB: Offline cache (Story 3.x in Epic 7)
- Background Sync: Queue pending changes when offline (Story 4.x in Epic 7)
- Offline Detection: This story (Story 0.3)

[Source: prd/epic-7-offline-first.md§Feature 7.1]
- Service Worker registration in main app
- Online/offline detection with visual feedback
- Foundation for IndexedDB and background sync

[Source: architecture/coding-standards.md§React Patterns]
- Use hooks for side effects (useEffect)
- Proper cleanup in useEffect return
- No missing dependency arrays
- SSR-safe implementations (check `typeof` before browser APIs)

### Project Structure

```
public/
└── sw.js                 # Service Worker (browser cache worker)

src/
├── hooks/
│   └── useOnlineStatus.ts     # Hook: detect online/offline state
├── components/
│   └── atoms/
│       └── OfflineIndicator.tsx  # Component: status badge
└── app/ or pages/
    └── layout.tsx (or _app.tsx)   # Register SW in root layout
```

### Service Worker Basics

- Service Workers run in background on a separate thread
- Cannot access DOM
- Can intercept network requests
- Can cache responses
- Can handle offline scenarios
- Must be served from HTTPS (localhost OK for dev)

### Browser Compatibility

- `navigator.serviceWorker` - modern browsers only
- Graceful fallback: if not available, app still works
- `navigator.onLine` - supported in all modern browsers

### Previous Story Context
Story 0.1 & 0.2 provide database and project structure. Story 0.3 adds offline infrastructure.

### Testing Standards

[Source: architecture/coding-standards.md§Testing Standards]
- Test framework: Jest
- Hook testing: @testing-library/react-hooks
- Test location: `src/**/__tests__/*.test.ts`
- For this story: Test useOnlineStatus hook behavior
- Test: online event changes state
- Test: offline event changes state
- Test: cleanup removes event listeners

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-03 | 1.0 | Story created | @sm (River) |

---

## Dev Agent Record

### Agent Model Used
_To be populated during implementation_

### Debug Log References
_To be populated during implementation_

### Completion Notes List
_To be populated during implementation_

### File List
_To be populated during implementation_

---

## QA Results

_To be populated during QA review_

---

**Story Status:** Draft  
**Created:** 2026-04-03  
**Depends On:** Story 0.2 (components folder structure)  
**Blocks:** Story 3.1+ (Epic 7 - IndexedDB and background sync)  
**Template:** AIOX Story Template v2.0
